# Phase-two user features

Authentication, accounts, saved cards, saved searches, alerts, sharing,
personalization, premium offers and private coupons are intentionally absent
from the MVP runtime. Add them only with a real backend identity provider and
protected API contract. Never restore localStorage demo authentication.
