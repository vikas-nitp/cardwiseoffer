# Frontend Analytics — 2026-07-12

**Deferred.** Vendor-neutral analytics layer (`src/analytics/{provider,events,index}.ts`) and GA4 loader gated on `VITE_GA_MEASUREMENT_ID` not added this pass. Event catalogue and non-PII property list is captured in the prompt spec §22 and will be implemented alongside the repository split.
